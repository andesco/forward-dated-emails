{
  "name": "forward-dated-emails",
  "version": "1.0.0",
  "description": "Cloudflare Email Worker for date-based email forwarding",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.local.toml",
    "dev": "wrangler dev --config wrangler.local.toml",
    "tail": "wrangler tail --config wrangler.local.toml"
  },
  "keywords": ["cloudflare", "workers", "email"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
