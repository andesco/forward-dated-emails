{
  "name": "expiring-email-routing",
  "version": "1.0.0",
  "description": "Cloudflare Email Worker for date-based email forwarding",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "dev": "wrangler dev --config wrangler.toml",
    "tail": "wrangler tail --config wrangler.toml"
  },
  "keywords": ["cloudflare", "workers", "email"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
